# ============================================================
# SWING TRADE DASHBOARD — Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values.
# The app reads these as environment variables.
# ============================================================

# --- Email Settings (Gmail) ---
# Your Gmail address (the sender)
SMTP_USER=your-email@gmail.com

# Gmail App Password (NOT your regular password)
# To create one: Google Account → Security → 2-Step Verification → App Passwords
# Generate a new app password and paste it here (16 characters, no spaces)
SMTP_PASSWORD=abcd efgh ijkl mnop

# Recipient email
EMAIL_TO=el.capitan.44@gmail.com

# SMTP server (defaults to Gmail)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# --- App Settings ---
PORT=5000
OUTPUT_DIR=output
